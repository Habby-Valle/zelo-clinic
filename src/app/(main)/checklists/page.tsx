import { ChecklistsPageClient } from "@/features/checklists/components";

export const metadata = { title: "Checklists — Zelo Clinic" };

export default function ChecklistsPage() {
  return <ChecklistsPageClient />;
}
